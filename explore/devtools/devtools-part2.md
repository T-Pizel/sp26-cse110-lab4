## Debugging Questions

1. The inputs have string types, so num1 + num2 performs string concatenation and not numeric addition.
2. I would fix the code by making the variables numbers instead of string values.